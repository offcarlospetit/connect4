import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const AutomaticPayment = (props: Props) => {
    return (
        <View>
            <Text>AutomaticPayment</Text>
        </View>
    );
};

export default AutomaticPayment;

const styles = StyleSheet.create({});